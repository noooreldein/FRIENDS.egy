HEAD
# متجر المنتجات الطبية - Medical Products E-commerce

موقع تجارة إلكترونية متخصص في بيع المنتجات الطبية مع دعم الدفع عبر واتساب والدفع عند التسليم.

## الميزات الرئيسية

- 🏥 **تصميم طبي**: تصميم مناسب للمنتجات الطبية مع ألوان طبية
- 🔐 **نظام المصادقة**: تسجيل الدخول وإنشاء الحسابات
- 🛒 **سلة التسوق**: إدارة المنتجات في السلة
- 💳 **طرق الدفع**: واتساب والدفع عند التسليم
- 👨‍💼 **لوحة الإدارة**: إدارة المنتجات والطلبات للمدراء
- 📱 **تصميم متجاوب**: يعمل على جميع الأجهزة
- 🌐 **لغة عربية**: واجهة باللغة العربية

## التقنيات المستخدمة

- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **Backend**: Supabase (PostgreSQL + Auth + Storage)
- **Deployment**: Vercel
- **Icons**: Lucide React

## متطلبات التشغيل

- Node.js 18+
- npm أو yarn
- حساب Supabase

## التثبيت والإعداد

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd medical-ecommerce
```

### 2. تثبيت التبعيات

```bash
npm install
```

### 3. إعداد Supabase

1. أنشئ مشروع جديد على [Supabase](https://supabase.com)
2. احصل على مفاتيح API:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 4. إعداد متغيرات البيئة

أنشئ ملف `.env.local` في المجلد الجذر:

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 5. إعداد قاعدة البيانات

1. في لوحة تحكم Supabase، انتقل إلى SQL Editor
2. قم بتشغيل محتوى ملف `supabase-schema.sql`

### 6. إنشاء حساب مدير

1. سجل حساب جديد عادي
2. في قاعدة البيانات، قم بتحديث دور المستخدم:

```sql
UPDATE public.profiles
SET role = 'admin'
WHERE email = 'your-admin-email@example.com';
```

### 7. تشغيل المشروع

```bash
npm run dev
```

## هيكل المشروع

```
src/
├── app/                    # Next.js App Router
│   ├── admin/             # صفحات الإدارة
│   ├── cart/              # صفحة السلة
│   ├── checkout/          # صفحة الدفع
│   ├── login/             # صفحة تسجيل الدخول
│   ├── register/          # صفحة التسجيل
│   ├── globals.css        # الأنماط العامة
│   ├── layout.tsx         # التخطيط الرئيسي
│   └── page.tsx           # الصفحة الرئيسية
├── components/            # المكونات المشتركة
│   ├── Header.tsx         # الشريط العلوي
│   ├── Footer.tsx         # التذييل
│   └── ProductGrid.tsx    # شبكة المنتجات
├── contexts/              # React Contexts
│   ├── AuthContext.tsx    # سياق المصادقة
│   └── CartContext.tsx    # سياق السلة
├── lib/                   # المكتبات المساعدة
│   └── supabase.ts        # إعداد Supabase
└── types/                 # تعريفات TypeScript
    └── index.ts           # الأنواع الأساسية
```

## طرق الدفع

### الدفع عبر واتساب
- يتم إرسال تفاصيل الطلب إلى رقم الواتساب: +201094708407
- يتم تأكيد الطلب والدفع عبر المحادثة

### الدفع عند التسليم
- يتم تسليم المنتج ودفع المبلغ نقداً
- مناسب للعملاء الذين يفضلون الفحص قبل الدفع

## إدارة المنتجات

### إضافة منتج جديد
1. انتقل إلى `/admin/products`
2. اضغط على "إضافة منتج جديد"
3. املأ التفاصيل وارفع الصورة
4. احفظ المنتج

### تحديث المنتج
1. انتقل إلى `/admin/products`
2. اضغط على أيقونة التعديل
3. قم بتحديث البيانات
4. احفظ التغييرات

## النشر

### على Vercel

1. اربط المشروع بـ GitHub
2. أضف متغيرات البيئة في إعدادات Vercel
3. انشر المشروع

### متغيرات البيئة المطلوبة

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

## المساهمة

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة
3. قم بالتعديلات
4. أرسل Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## الدعم

للاستفسارات أو المساعدة، يرجى التواصل عبر:
- البريد الإلكتروني: support@example.com
- الهاتف: +201094708407

# متجر المنتجات الطبية - Medical Products E-commerce

موقع تجارة إلكترونية متخصص في بيع المنتجات الطبية مع دعم الدفع عبر واتساب والدفع عند التسليم.

## الميزات الرئيسية

- 🏥 **تصميم طبي**: تصميم مناسب للمنتجات الطبية مع ألوان طبية
- 🔐 **نظام المصادقة**: تسجيل الدخول وإنشاء الحسابات
- 🛒 **سلة التسوق**: إدارة المنتجات في السلة
- 💳 **طرق الدفع**: واتساب والدفع عند التسليم
- 👨‍💼 **لوحة الإدارة**: إدارة المنتجات والطلبات للمدراء
- 📱 **تصميم متجاوب**: يعمل على جميع الأجهزة
- 🌐 **لغة عربية**: واجهة باللغة العربية

## التقنيات المستخدمة

- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **Backend**: Supabase (PostgreSQL + Auth + Storage)
- **Deployment**: Vercel
- **Icons**: Lucide React

## متطلبات التشغيل

- Node.js 18+
- npm أو yarn
- حساب Supabase

## التثبيت والإعداد

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd medical-ecommerce
```

### 2. تثبيت التبعيات

```bash
npm install
```

### 3. إعداد Supabase

1. أنشئ مشروع جديد على [Supabase](https://supabase.com)
2. احصل على مفاتيح API:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 4. إعداد متغيرات البيئة

أنشئ ملف `.env.local` في المجلد الجذر:

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 5. إعداد قاعدة البيانات

1. في لوحة تحكم Supabase، انتقل إلى SQL Editor
2. قم بتشغيل محتوى ملف `supabase-schema.sql`

### 6. إنشاء حساب مدير

1. سجل حساب جديد عادي
2. في قاعدة البيانات، قم بتحديث دور المستخدم:

```sql
UPDATE public.profiles
SET role = 'admin'
WHERE email = 'your-admin-email@example.com';
```

### 7. تشغيل المشروع

```bash
npm run dev
```

## هيكل المشروع

```
src/
├── app/                    # Next.js App Router
│   ├── admin/             # صفحات الإدارة
│   ├── cart/              # صفحة السلة
│   ├── checkout/          # صفحة الدفع
│   ├── login/             # صفحة تسجيل الدخول
│   ├── register/          # صفحة التسجيل
│   ├── globals.css        # الأنماط العامة
│   ├── layout.tsx         # التخطيط الرئيسي
│   └── page.tsx           # الصفحة الرئيسية
├── components/            # المكونات المشتركة
│   ├── Header.tsx         # الشريط العلوي
│   ├── Footer.tsx         # التذييل
│   └── ProductGrid.tsx    # شبكة المنتجات
├── contexts/              # React Contexts
│   ├── AuthContext.tsx    # سياق المصادقة
│   └── CartContext.tsx    # سياق السلة
├── lib/                   # المكتبات المساعدة
│   └── supabase.ts        # إعداد Supabase
└── types/                 # تعريفات TypeScript
    └── index.ts           # الأنواع الأساسية
```

## طرق الدفع

### الدفع عبر واتساب
- يتم إرسال تفاصيل الطلب إلى رقم الواتساب: +201094708407
- يتم تأكيد الطلب والدفع عبر المحادثة

### الدفع عند التسليم
- يتم تسليم المنتج ودفع المبلغ نقداً
- مناسب للعملاء الذين يفضلون الفحص قبل الدفع

## إدارة المنتجات

### إضافة منتج جديد
1. انتقل إلى `/admin/products`
2. اضغط على "إضافة منتج جديد"
3. املأ التفاصيل وارفع الصورة
4. احفظ المنتج

### تحديث المنتج
1. انتقل إلى `/admin/products`
2. اضغط على أيقونة التعديل
3. قم بتحديث البيانات
4. احفظ التغييرات

## النشر

### على Vercel

1. اربط المشروع بـ GitHub
2. أضف متغيرات البيئة في إعدادات Vercel
3. انشر المشروع

### متغيرات البيئة المطلوبة

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

## المساهمة

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة
3. قم بالتعديلات
4. أرسل Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## الدعم

للاستفسارات أو المساعدة، يرجى التواصل عبر:
- البريد الإلكتروني: support@example.com
- الهاتف: +201094708407
>>>>>>> b242b1431e2f7889bf4859690fb3516cb5900093
